export interface ClassData {
  id: string;
  name: string;
  room: string;
  section: string;
  subject: string;
  password: string;
  userId: string;
  adminList: string[];
  createdAt: string; // ISO date string
  updatedAt: string; // ISO date string
}
